import * as React from 'react';

interface IProps { };
interface IState { };

export default class Body extends React.Component<IProps, IState> {
  render() {
    return (<main>
      Body
    </main>);
  }
}
