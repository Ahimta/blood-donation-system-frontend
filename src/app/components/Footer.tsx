import * as React from 'react';

interface IProps { };
interface IState { };

export default class Footer extends React.Component<IProps, IState> {
  render() {
    return (<footer>
      Footer
    </footer>);
  }
}
